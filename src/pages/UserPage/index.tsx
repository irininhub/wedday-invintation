import { FC } from "react";
import { InvationLayout } from "../../layouts";
import { InvationModule } from "../../modules";

export const UserPage: FC = () => {
    return (
        <InvationLayout>
            <InvationModule />
        </InvationLayout>
    );
};
